<%= form_with(model: flight, local: true) do |form| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :origin_id %>
    <%= form.collection_select :origin_id, City.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :destination_id %>
    <%= form.collection_select :destination_id, City.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :airplane_id %>
    <%= form.collection_select :airplane_id, Airplane.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
